import { makeStyles } from 'tss-react/mui';

export const useStyles = makeStyles()(() => ({
    shortBtn: {
        marginTop: '1.7em',
    },
    marginContainer: {
        margin: '2%',
        alignItems: 'left',
    },
}));
